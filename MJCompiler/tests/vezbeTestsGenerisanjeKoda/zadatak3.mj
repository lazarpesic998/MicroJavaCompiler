program ABC
{
    void main()
        int x,y;
    {
        if(x==1) y=2;
        else y=3;

        for(x=0; x<3; x++) print(x);

        while(x<4){
            if(x==1) continue;
            else if(x==2) break;
            else y=0;
            y=y%2;
        }

        if(x>0 && y<2) x=2;
        if(x>=0 || y<=2) x=3;
        if(x!=0 &&)
    }
} 